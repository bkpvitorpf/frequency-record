// Cria e exporta um histórico de navegação para que os componentes externos tenham acesso ao histórico de navegação do usuário

import {createBrowserHistory} from 'history';

export default createBrowserHistory();